apiVersion: batch/v1
kind: Job
metadata:
  generateName: gamal-job-
spec:
  template:
    spec:
      containers:
      - name: jobtest
        image: busybox
        command: ["sleep", "180"] 
      restartPolicy: Never
